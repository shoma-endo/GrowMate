# 要件定義テンプレート

このテンプレートをコピーして、実装対象の `docs/plans/<slug>.md` を作成する。
空欄を残す場合は、`該当なし` または `未確定` と理由・確認者・確認期限を記載する。
要件定義では「何を作るか」「どこまで作るか」「何をもって完了とするか」を決め、具体的な実装方法は設計セクションに分ける。

## メタデータ

- 文書名:
- ステータス: `draft` / `review` / `approved` / `implemented`
- 作成日:
- 最終更新日:
- 作成者:
- 承認者:
- 対象リリース:
- 関連する依頼・Issue・PR:

## 1. 背景・目的・成功指標

### 背景・解決したい課題

- 現在、誰が、どの業務で困っているか:
- 放置した場合の影響:

### 目的

- この開発で実現する状態:
- 利用者・事業にとっての価値:

### 成功指標

| 指標 | 現状 | 目標 | 測定方法 | 測定時期 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 2. 利用者・関係者・利用シナリオ

| 区分 | 対象 | 期待すること・責任 |
| --- | --- | --- |
| 利用者 |  |  |
| 運用担当 |  |  |
| 管理者・承認者 |  |  |
| 外部サービス・連携先 |  |  |

### 主な利用シナリオ

1. **誰が**、**どの状況で**、**何をしたいか**:
2. **誰が**、**どの結果を確認するか**:

## 3. 業務要件と業務フロー

### 現状（As-Is）

```text
現状の業務フローを記載する。
```

### 導入後（To-Be）

```text
導入後の業務フローを記載する。
```

### 業務ルール

- ルール ID:
- ルール:
- 例外:

## 4. 対象範囲と Non-goals

### 対象範囲

- 画面・操作:
- API・外部連携:
- データ・DB:
- 権限・ロール:
- 運用・監視:

### Non-goals（今回の対象外）

- 対象外にするもの:
- 対象外にする理由:
- 将来検討する条件・時期:

## 5. 機能要件

| ID | 機能要件 | 優先度 | 根拠・出典 | 受け入れ条件 |
| --- | --- | --- | --- | --- |
| FR-001 |  | Must / Should / Could / Won't |  |  |

### 入力・出力・状態遷移

- 入力値・形式・必須条件:
- 正常時の出力:
- エラー時の出力:
- 状態と遷移条件:
- 冪等性・重複実行時の挙動:

### 権限

| ロール | 閲覧 | 作成・実行 | 更新 | 削除・解除 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 6. Gherkin受け入れ条件

承認済みの `02-gherkin.md` をここへ反映する。Gherkinは機能要件の検証可能な振る舞いを表し、実装クラス名やDB内部名には依存させない。

```gherkin
Feature: 機能名

  Rule: 業務ルール。不要なら省略

  Scenario: 正常系の振る舞い
    Given 前提条件
    When 利用者が操作する
    Then 期待される結果
```

### シナリオ対応表

| シナリオ | 対応する機能要件 | 対応する決定事項 |
| --- | --- | --- |
|  | FR-001 |  |

## 7. 非機能要件

該当しない項目も `対象外` と理由を記載する。未検討の項目は `未確定` とし、確認期限を置く。

| 分類 | 要件・目標値 | 検証方法 | 状態・根拠 |
| --- | --- | --- | --- |
| 性能・レイテンシ | 主要操作の応答時間、同時利用者数、バッチ所要時間 |  |  |
| 可用性・信頼性 | 稼働時間、許容停止時間、冗長化、リトライ |  |  |
| セキュリティ・プライバシー | 秘密情報、個人情報、暗号化、保持・削除 |  |  |
| 認証・認可 | ロール、RLS、管理者操作、セッション |  |  |
| 監査・ログ | 記録対象、保持期間、マスキング |  |  |
| 障害対応 | 検知、通知、復旧目標（RTO/RPO）、再実行 |  |  |
| バックアップ・復旧 | 対象、頻度、復旧手順、復旧確認 |  |  |
| 運用・監視 | メトリクス、アラート、担当者、保守時間 |  |  |
| 拡張性・互換性 | 将来の規模、対応ブラウザ・端末、API互換 |  |  |
| アクセシビリティ | キーボード操作、コントラスト、スクリーンリーダー |  |  |
| コスト | API利用料、ストレージ、実行回数、上限 |  |  |

### AI機能の追加観点

| 項目 | 要件・目標値 | 検証方法 | 状態・根拠 |
| --- | --- | --- | --- |
| 出力品質・評価基準 |  |  |  |
| 禁止事項・安全性 |  |  |  |
| 人間の確認・上書き |  |  |  |
| モデル・プロバイダ障害時 |  |  |  |
| トークン・コスト上限 |  |  |  |
| レイテンシ・タイムアウト |  |  |  |

## 8. データ・外部連携

### データ

- 作成・更新・削除するデータ:
- データの所有者:
- 保持期間・削除条件:
- 移行・既存データとの互換性:
- RLS・Service Role・ユーザー境界:

### 外部連携

| 連携先 | 用途 | API・権限 | 失敗時の挙動 | 公式根拠 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 9. 制約・前提・依存関係

### 技術前提

- 既存システム・ライブラリ・社内標準:
- 再利用する既存実装:

### 制約条件

- 納期・予算・人員:
- 法令・契約・審査:
- 変更できない既存仕様:

### 依存関係

| 依存対象 | 前提条件 | 完了確認 | 未完了時の影響 |
| --- | --- | --- | --- |
|  |  |  |  |

## 10. トレードオフ判断

重要な設計・スコープ判断は、採用理由と却下案を残す。後から判断を変更する条件も明記する。

### 判断 1: 判断名

- 判断:
- 比較した案:
  - 案A:
  - 案B:
- 採用案:
- 採用理由:
- 却下した案と理由:
- 影響（コスト・納期・品質・運用・拡張性）:
- 将来変更する条件:
- 判断者・判断日:

## 11. リスク・未確定事項・確認質問

### リスク

| ID | リスク | 発生条件・影響 | 対策 | 担当 | 状態 |
| --- | --- | --- | --- | --- | --- |
| R-001 |  |  |  |  |  |

### 未確定事項

| ID | 質問・未確定事項 | 回答が必要な理由 | 回答者 | 期限 | 状態 |
| --- | --- | --- | --- | --- | --- |
| Q-001 |  |  |  |  | 未回答 |

## 12. テスト・リリース・ロールバック

### テスト方針

- 単体・統合・E2E・実画面確認:
- Gherkinシナリオとの対応:
- 外部API・失敗系・境界条件:
- セキュリティ・権限・RLS:
- 非機能要件の測定:

### リリース方針

- リリース単位・段階展開:
- Feature Flag / allowlist:
- データベース変更の適用順序:
- 本番確認項目:

### ロールバック方針

- アプリケーションの戻し方:
- DB変更の戻し方・逆マイグレーション:
- データ不整合時の復旧:
- ロールバック判断者:

## 13. 承認・変更履歴

### 承認

| 役割 | 氏名 | 判定 | 日付 | コメント |
| --- | --- | --- | --- | --- |
| 要件承認者 |  | 未承認 |  |  |
| 技術レビュー |  | 未承認 |  |  |

### 変更履歴

| 日付 | 変更内容 | 変更理由 | 変更者 |
| --- | --- | --- | --- |
|  |  |  |  |
