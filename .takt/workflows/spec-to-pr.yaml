name: spec-to-pr
description: GrowMate docs/plans specification to implementation, quality gates, Codex architecture review loop, commit, and push for auto PR.
workflow_config:
  provider_options:
    codex:
      network_access: true
max_steps: 26
initial_step: plan
loop_monitors:
  - cycle:
      - ai_antipattern_review
      - ai_antipattern_fix
    threshold: 3
    judge:
      persona: supervisor
      knowledge:
        - growmate
      instruction: loop-monitor-ai-antipattern-fix
      rules:
        - condition: 健全（進捗あり）
          next: ai_antipattern_review
        - condition: 非生産的（改善なし）
          next: review
  - cycle:
      - review
      - fix
    threshold: 3
    judge:
      persona: supervisor
      knowledge:
        - growmate
      instruction: |
        review -> fix のループが {cycle_count} 回繰り返されました。
        Report Directory の最新レビューと修正結果を確認し、収束しているか判断してください。
        残件が「テスト追加要求」のみで、対象仕様書または GrowMate knowledge がテスト追加不要を明示している場合は、追加修正で解消不能な方針差分として self_review に進めてください。
      rules:
        - condition: 収束済み（残件が追加修正で解消不能な方針差分のみ）
          next: self_review
        - condition: 健全（進捗あり）
          next: review
        - condition: 非生産的（同じ指摘の反復・修正未反映）
          next: ABORT

steps:
  - name: plan
    tags:
      - plan
    edit: false
    persona: planner
    knowledge:
      - architecture
      - growmate
    provider_options:
      extends: review-readonly
    instruction: |
      `docs/plans/` の仕様書を起点に、GrowMate の実装計画を作成してください。

      必須手順:
      1. ユーザー指示から対象仕様書を特定する。曖昧な場合は `docs/plans/` を列挙し、候補を示して ABORT する。
      2. 対象仕様書、`AGENTS.md`、`.takt/facets/knowledge/growmate.md`、`.agents/skills/implementation-guidelines/SKILL.md` を読む。
      3. Server Actions / Route Handlers / Zod / エラー処理が関わる場合は `.agents/skills/nextjs-server/SKILL.md` を読む。
      4. UI 実装が関わる場合は `.agents/skills/growmate-ui-ux/SKILL.md` と `.agents/skills/react/SKILL.md` を読む。
      5. Supabase / migration / RLS / Service Role が関わる場合は `.agents/skills/supabase/SKILL.md` を読む。
      6. 仕様要件と現行コードの差分、変更対象、検証方法、PR本文に記載すべき関連仕様書を整理する。
      7. 対象仕様書が spec-review 済みか確認する（`.takt/workflows/spec-review.yaml` のレビューレポートまたは docs PR）。未実施の場合はブロックしないが、計画レポートに「spec-review 未実施」と明記する。
    rules:
      - condition: 要件が明確で実装可能
        next: implement
      - condition: ユーザーが質問をしている（実装タスクではない）
        next: COMPLETE
      - condition: 要件が不明確、情報不足
        next: ABORT
        appendix: |
          確認事項:
          - {質問1}
          - {質問2}
    output_contracts:
      report:
        - name: plan.md
          format: plan

  - name: implement
    tags:
      - coding
      - implementation
    edit: true
    persona: coder
    policy:
      - coding
    knowledge:
      - architecture
      - growmate
    provider_options:
      extends: edit
    required_permission_mode: edit
    instruction: |
      `plan.md` と対象仕様書に従い、GrowMate の既存パターンに合わせて最小差分で実装してください。

      必須条件:
      - `any` を使わない。`unknown` + narrowing または Zod schema を使う。
      - 自動生成ファイルを直接編集しない。
      - UI 変更では `growmate-ui-ux` と `react` の規約に従う。
      - Server Actions / Route Handlers では `nextjs-server` のエラー処理・Zod規約に従う。
      - Supabase Service Role 使用時は user_id 等の明示的なスコープ条件を入れる。
      - 簡易・形式的・低価値なユニットテストは追加しない。テスト追加は対象仕様書またはユーザー指示で明示されている場合に限る。
      - 実装開始前に対象仕様書の現行版を再読し、`plan.md` 作成時点の内容と食い違いがないか確認する。食い違いがあれば実装を進めず plan への差し戻しを判断する。
      - 実装後に `npm run verify` を実行し、失敗した場合は修正して再実行する。
    rules:
      - condition: 実装と必須検証が完了した
        next: ai_antipattern_review
      - condition: 対象仕様書が plan.md 作成後に変更されており、実装方針の再検討が必要
        next: plan
      - condition: 判断できない、情報不足
        next: ABORT
      - condition: ユーザー入力が必要
        next: implement
        requires_user_input: true
        interactive_only: true
    output_contracts:
      report:
        - name: coder-scope.md
          format: coder-scope
        - name: coder-decisions.md
          format: coder-decisions

  - name: ai_antipattern_review
    tags:
      - review
    edit: false
    persona: ai-antipattern-reviewer
    policy:
      - review
      - ai-antipattern
    knowledge:
      - growmate
    provider_options:
      extends: review-readonly
    instruction: ai-antipattern-review
    rules:
      - condition: AI特有の問題が見つからない
        next: review
      - condition: AI特有の問題が検出された
        next: ai_antipattern_fix
    output_contracts:
      report:
        - name: ai-antipattern-review.md
          format: ai-antipattern-review

  - name: ai_antipattern_fix
    tags:
      - coding
      - implementation
    edit: true
    persona: coder
    policy:
      - coding
    knowledge:
      - architecture
      - growmate
    provider_options:
      extends: edit
    required_permission_mode: edit
    pass_previous_response: false
    instruction: ai-antipattern-fix
    rules:
      - condition: AI Reviewerの指摘に対する修正が完了した
        next: ai_antipattern_review
      - condition: 修正不要（指摘対象ファイル/仕様の確認済み）
        next: review
      - condition: 修正を進行できない
        next: review

  - name: review
    tags:
      - review
    edit: false
    persona: architecture-reviewer
    knowledge:
      - architecture
      - growmate
    instruction: |
      `review-arch` 相当の観点で、アーキテクチャ、責務分離、データ境界、セキュリティ、仕様充足、既存パターンとの整合性をレビューしてください。

      GrowMate 固有条件:
      - 対象仕様書または GrowMate knowledge がテスト追加不要を明示している場合、テスト不在のみを理由に `needs_fix` にしない。
      - 簡易・形式的・低価値なユニットテストの追加を要求しない。
      - 代替検証として `npm run verify`、必要な手動確認が実行されているかを確認する。
      - 実装上の correctness / security / data integrity / architecture 問題がある場合は具体的に `needs_fix` とする。
    rules:
      - condition: approved
        next: self_review
      - condition: needs_fix
        next: fix
    output_contracts:
      report:
        - name: architecture-review.md
          format: architecture-review

  - name: fix
    tags:
      - coding
      - implementation
    edit: true
    persona: coder
    policy:
      - coding
    knowledge:
      - architecture
      - growmate
    provider_options:
      extends: edit
    required_permission_mode: edit
    pass_previous_response: false
    instruction: |
      Report Directory の `architecture-review.md` と `self-review.md` を一次情報として、未解決の指摘だけを修正してください。

      必須条件:
      - 修正着手前に対象仕様書の現行版を確認し、レビュー指摘の範囲を超える仕様変更がないか確認する。
      - architecture-reviewer の未解決指摘がある場合は、指摘対象コードを直接修正する。
      - self_review の未解決指摘がある場合は、self-review.md の該当項目を優先して修正する。
      - architecture-review.md が APPROVE で self_review 起因の差し戻しだけの場合、architecture review を再解決しようとして同じ確認を繰り返さない。
      - 修正対象が git hygiene / 不要ファイル混入 / レポート不足 / 検証証跡不足だけの場合は、その対象だけを直す。
      - テストやドキュメント追加だけで architecture-reviewer の指摘を回避しない。
      - 簡易・形式的・低価値なユニットテストは追加しない。テスト追加は対象仕様書またはユーザー指示で明示されている場合に限る。
      - 同種の潜在箇所がある場合は同時に修正する。
      - プロダクションコードを変更した場合は `npm run verify` を再実行する。
      - プロダクションコードを変更しておらず、直前の品質ゲート成功証跡がある場合は、フル再実行せず必要な git / diff 確認に絞ってよい。
      - ここでは commit / push を行わない。最終ステップでまとめて実施する。
    rules:
      - condition: architecture-reviewer の指摘を修正し、必須検証が完了した
        next: review
      - condition: self_review 起因の指摘だけを修正し、architecture-reviewer の open findings がない
        next: self_review
      - condition: レビュー指摘に対応するための情報が不足している
        next: plan
      - condition: 対象仕様書がレビュー後に変更されており、指摘の枠を超える実装方針の再検討が必要
        next: plan
      - condition: 修正を進行できない
        next: ABORT
    output_contracts:
      report:
        - name: fix-result.md
          format: coder-decisions

  - name: self_review
    tags:
      - review
    edit: false
    persona: supervisor
    knowledge:
      - architecture
      - growmate
    provider_options:
      extends: review-readonly
    instruction: |
      `.agents/skills/quality-gate/SKILL.md` と `.agents/skills/quality-gate/self-review.md` に従い、2パスセルフレビューを実施してください。

      確認対象:
      - 仕様書の全要件が実装されていること。
      - `npm run verify` が成功していること。
      - architecture-reviewer の open findings がないこと。
      - 余計な差分や機密情報の露出がないこと。
      - PR確認用の要約に関連仕様書、検証結果、Codex architecture review 結果を含められること。
    rules:
      - condition: セルフレビュー合格
        next: prepare_pr_summary
      - condition: 修正が必要
        next: fix
      - condition: 検証不足または判断不能
        next: ABORT
    output_contracts:
      report:
        - name: self-review.md
          format: supervisor-validation

  - name: prepare_pr_summary
    tags:
      - report
    edit: false
    persona: reporter
    knowledge:
      - growmate
    provider_options:
      extends: review-readonly
    instruction: |
      push 前に、Report Directory と source run chain の最新レポートから、最終ローカル要約を短く作成してください。

      必須条件:
      - 実装やレビュー判断は行わない。確認済みの事実と未確認事項を分けて整理する。
      - 読むレポートは原則として最新の `architecture-review.md`、`self-review.md`、`fix-result.md`、必要に応じて `plan.md` までに絞る。
      - `coder-scope.md` / `coder-decisions.md` は存在する場合だけ参照し、存在しなくても不足レポートとして扱わない。
      - 要約は 概要 / 関連仕様書 / 検証結果 / review結果 / 未確認事項 / コミットメッセージ案 に絞る。
      - 変更ファイルの詳細表は作らない。必要なら `git status --short` の件数と主要カテゴリだけにする。
      - Requeue / resume で現在の Report Directory に必要レポートが存在しない場合は、現在 run の `meta.json` にある `source_run_slug` を辿り、source run の `reports/` にある最新レポートを参照する。source run も Requeue の場合は、さらに `source_run_slug` を辿る。
      - 現在 run または source run chain のどこにも `architecture-review.md` または `self-review.md` が存在せず、レビュー結果を確認できない場合のみ不足レポートとして扱う。
    rules:
      - condition: 最終ローカル要約が完了した（source run のレポート参照を含む）
        next: create_pr
      - condition: architecture-review または self-review の結果を確認できない
        next: ABORT
    output_contracts:
      report:
        - name: pr-summary.md
          format: summary

  - name: create_pr
    tags:
      - coding
    edit: true
    persona: coder
    provider: claude
    model: haiku
    knowledge:
      - growmate
    required_permission_mode: edit
    provider_options:
      claude:
        allowed_tools:
          - Read
          - Glob
          - Grep
          - Bash
    instruction: |
      実装内容をコミットし、ブランチへ push してください。Pull Request 作成は `.github/workflows/auto-pr.yml` に委譲するため、ここでは `gh pr create` / `gh pr edit` / `gh pr comment` を実行しないでください。プロダクションコードの編集は行わず、git コマンドのみを実行してください。

      手順:
      1. `git status` / `git diff` で変更内容を確認する。
      2. 現在のブランチが `develop` または `main` なら、仕様書名または機能名から `feature/...` または `fix/...` の英語ブランチ名を作成する。
      3. `git add` で変更をステージし、`git diff --cached` で最終確認する。
      4. Why を中心とした日本語1行のコミットメッセージで `git commit` する。
      5. `git push -u origin HEAD` を実行する。
      6. push 後、`.github/workflows/auto-pr.yml` が PR を作成・更新する前提で、ブランチ名とコミット SHA を報告する。
      7. `pr-summary.md` はローカルの確認用レポートとして扱い、PR 本文の更新は行わない。

      やらないこと:
      - `gh pr create` / `gh pr edit` / `gh pr comment`。
      - PR の作成・更新・マージ・クローズ。
      - プロダクションコード編集。
    rules:
      - condition: コミットと push が完了した
        next: COMPLETE
      - condition: コミットまたは push に失敗した
        next: ABORT
