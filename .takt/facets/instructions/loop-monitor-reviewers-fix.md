ai_antipattern_review -> ai_antipattern_fix のループが {cycle_count} 回繰り返されました。

下記に全文添付された最新のレビュー報告と、Previous Response（直近ステップの報告。切り詰められている場合があります）を突き合わせ、判定してください。run ディレクトリやレポートパスの探索はしないでください。

**最初に観測時点を確定してください:**
- 添付の最新レビューは、直近の修正より前の時点の内容である可能性があります。Previous Response に当該指摘を修正したとの報告があり、その内容が添付レビューの指摘に対応している場合は「修正後の検証待ち」です。
- 当該指摘を修正したとの報告がないまま、同じ指摘が繰り返されている場合は停滞です。

**判断基準:**
- **修正進捗:** 前回の open findings の受入条件が満たされたかを、修正報告と現行コードで確認してください。同じ `finding_id`、根本原因、または受入条件が修正後も繰り返される場合は、実装が未完了です。
- **報告の収束:** 完了したレビュー回を比較し、正しい構造・契約上の new findings が追加され続けていないか確認してください。
- 残件が AI 特有の antipattern（幻覚 API、無意味な抽象化、過剰なコメント、偽の完了宣言など）ではなく、アーキテクチャ・仕様充足・セキュリティ・テスト方針の論点である場合は、このループで解消できません。architecture review に委譲してください。
- 残件が軽微な nits だけで、もう1回の antipattern review が新しい AI 特有問題を生む見込みが低い場合も委譲してください。
- 件数は補助情報に留め、修正進捗と報告内容の収束を別々に判定してください。

**判定は次のいずれか1つで終えてください（文言を残す）:**
- 収束済み（残件が AI 特有問題ではない、または architecture review に委譲可能）
- 健全（進捗あり）
- 非生産的（同じ指摘の反復・修正未反映）

実装未完了または報告未収束であり、利用可能な打開手段がない場合に限り、非生産的を選んでください。

## ai-antipattern-review.md（最新・全文）
{report:ai-antipattern-review.md}
