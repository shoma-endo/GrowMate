```markdown
# タスク計画

対象仕様書: docs/plans/<slug>.md
ステータス: approved|implemented
対象スコープ: {本ランの実装範囲。例: フェーズ0+1 / 全文。Non-goals は書かない}
UIモック: 対象外|なし|あり（ゲート: なし|承認済み）

## 元の要求
{ユーザーの要求を逐語のまま記載。分析、現行実装、レビュー提案、設計判断を追加しない}

## 分析結果

### 目的
{達成すべきこと}

### 分解した要件
| # | 要件 | 変更要否 | 種別 | 由来・導出根拠 | 備考 |
|---|------|----------|------|----------------|------|
| 1 | {要件1} | 要 / 不要 — 不要の場合は現行コードの根拠を `file:line` で記載 | 明示 / 直接導出 / 維持 | {明示要求のソース箇所 / 成立に必要な明示要求と理由 / 変更対象外にある観測可能な既存契約の証拠} | {複合要件を分解した場合の補足} |

- 1つの文に複数条件がある場合、検証可能な最小単位まで分解する
- `A/B`、`global/project`、`JSON/leaf`、`allow/deny`、`read/write` のような並列表現は別行にする
- ソースが複数の方法を許容する場合は、名前が挙がった各方式と同等手段を認める開放選択肢をすべて維持し、採用案は「検討したアプローチ」に記載する
- 設計判断や内部構造の候補を要件として追加しない
- 置換時は、現行利用側の移行、旧経路削除、明示された各支援対象を別々の完了契約行にする
- 複数の許容案から一案を選んだ場合、完了契約は許容集合と明示された維持範囲だけを固定する。選んだ案を、維持要求が及ばない入力や経路まで固定する独立契約にしない
- 維持義務が特定の入力、モード、状態、または入口だけに適用される場合は、契約を適用範囲ごとに分ける。ソースが選択肢を開いたままにしている範囲へ、現行動作や採用案を拡張しない
- Original Task Context を要求の正本とし、Previous Work Context は調査結果と計画案として扱う。Previous Work Context にあるだけの優先順位、既定値、設定形式、識別子、エラー時動作、ライフサイクルを要求や完了契約へ昇格させない
- 最終化前に各要件と完了契約の由来を再確認し、明示要求、不可欠な直接導出、変更対象外として維持する観測可能な既存契約のいずれでもない項目は「検討したアプローチ」または「確認事項」へ移す
- ファイル参照は作業ディレクトリからの相対パスで記載し、ホームディレクトリやworktreeの絶対パスを含めない

### 参照資料の調査結果（参照資料がある場合）
{参照資料の実装アプローチの概要と、現在の実装との主要な差異}

### スコープ
{影響範囲}

### 検討したアプローチ（設計判断がある場合）
| アプローチ | 採否 | 理由 |
|-----------|------|------|

### 実装アプローチ
{どう進めるか}

### 完了契約
| 契約ID | 要求・維持事項 | 由来 | 成立する振る舞い | 拒否すべき誤実装 | 実装箇所 | 完了証拠 |
|--------|----------------|------|------------------|--------------------|----------|----------|
| `{安定ID}` | {明示要求、直接導出した要求、または変更対象外として維持する観測可能な既存契約} | {分解した要件の行と根拠} | {観測可能な成功条件} | {もっともらしい反例} | {変更候補} | {観測方法と検証レイヤー} |

### 影響経路（該当する契約のみ）
| 契約ID | 定義・生成 | 変換・保存・復元 | 消費・出力・補助入口 | 状態・所有権 | 現行利用側の移行 | 明示された支援 |
|--------|------------|------------------|---------------------|-------------|------------------|------------------|
| `{同じ安定ID}` | {定義元と生成元} | {実在する中間経路} | {全消費元と入口} | {状態・所有権} | {置換時に新契約へ移す利用側} | {要求された場合だけ対象・範囲} |

### 到達経路・起動条件（利用者向け機能の追加/変更がある場合）
| 項目 | 内容 |
|------|------|
| 利用者が到達する入口 | {メニュー/route/ボタン/リンク/外部呼び出し など。なければ「入口なし」と明記} |
| 更新が必要な呼び出し元・配線 | {更新対象のファイルや層} |
| 起動条件 | {認証、権限、URL条件、フラグなど} |
| 未対応項目 | {残っている配線があれば明記。なければ「なし」} |

## 実装ガイドライン（設計が必要な場合のみ）
- {Coderが実装時に従うべき指針}

## スコープ外（項目がある場合のみ）
| 項目 | 除外理由 |
|------|---------|

## 確認事項（あれば）
- {不明点や確認が必要な点}
```
