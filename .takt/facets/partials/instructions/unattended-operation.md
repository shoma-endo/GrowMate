**運用前提（無人実行）:**
- 本工程では人間への質問・確認待ちを行わない。判断不能・仕様不足なら ABORT する（人間が仕様書を直して `spec-review` から再実行する前提）。
- 検証ゲートは `npm run verify` と TAKT 内レビューとする。ブラウザ手動確認・実DB/RLS実環境・外部API/LLM実通信・マイグレーションのリモート適用は無人運用の検証対象外であり、未実施を失敗・未完了・ABORT・不合格の理由にしない。PR の未確認事項として記録して先へ進む。
