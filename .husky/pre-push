# husky v9 はフックを `sh -e <hook>` で起動する（.husky/_/h）ためシェバンが効かない。
# bash 専用構文をここに書くと /bin/sh が dash の環境で落ちるので、本体は bash 明示で呼ぶ。
# 中身は scripts/pre-push.sh（takt pin ガード → test:coverage → build → knip）。
bash scripts/pre-push.sh "$@"
